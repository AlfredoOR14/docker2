pipeline {
    agent any

    environment {
        DOCKERFILE_PATH = '.'
        
        WORKSPACE_PATH = 'var/lib/jenkins/workspace/docker'
    }

    stages {
        stage('Build Docker Image') {
            steps {
                sh "docker build -t ${DOCKERFILE_PATH} -f ${DOCKERFILE_PATH}/Dockerfile ."
            }
        }
    }
}
